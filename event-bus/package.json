{
	"name": "event-bus",
	"module": "index.ts",
	"type": "module",
	"scripts": {
		"watch": "biome check --write . && tsc --noEmit && (bun --watch index.ts & tsc --noEmit --watch --preserveWatchOutput)",
		"typecheck": "tsc --noEmit",
		"check": "biome check . && tsc --noEmit",
		"fix": "biome check --write ."
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.12",
		"@types/bun": "^1.4.1",
		"typescript": "^6.0.0"
	},
	"dependencies": {
		"axios": "^1.20.0",
		"hono": "^4.13.7"
	}
}
