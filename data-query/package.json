{
	"name": "data-query",
	"module": "index.ts",
	"type": "module",
	"scripts": {
		"watch": "biome check --write . && bun run check:effect && (bun --watch index.ts & tsc --noEmit --watch --preserveWatchOutput)",
		"test": "bun test",
		"typecheck": "tsc --noEmit",
		"check:effect": "effect-language-service diagnostics --project tsconfig.json --format pretty",
		"check": "biome check . && tsc --noEmit && bun run check:effect && bun test",
		"fix": "biome check --write ."
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.0",
		"@effect/language-service": "^0.87.2",
		"@types/bun": "latest",
		"typescript": "^5.0.0"
	},
	"dependencies": {
		"effect": "^4.0.0-rc.112"
	}
}
